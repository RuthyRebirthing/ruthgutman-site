# CLAUDE.md — אתר רותי גוטמן · ריברסינג

מדריך מבנה מלא לאתר. המטרה: שכל שינוי עתידי ייעשה מהר ונכון, בלי לחקור מחדש.

---

## 1. מה זה האתר

אתר תדמית + שיווק ל**רותי גוטמן**, מטפלת רגשית ב**ריברסינג (נשימה טיפולית)** מאשדוד.
כולל תוכן שיווקי/SEO, טופס יצירת קשר, ומערכת **שובר מתנה** (רכישה בביט + הנפקת קופון סידורי).

- **דומיין חי:** https://ruthgutman.co.il
- **טלפון/וואטסאפ עסקי:** 053-2771754 (בינ"ל: `972532771754`)
- **מייל עסקי:** `r0532771754@gmail.com`
- **קהל:** ילדים, נערות, נשים, אמהות אחרי לידה. שפה: **עברית, RTL בלבד**.

---

## 2. סטאק ואירוח — אין build!

- **HTML/CSS/JS סטטי לגמרי (Vanilla).** אין framework, אין npm, אין שלב build, אין bundler.
- **עריכה = עריכה ישירה של הקובץ.** אחרי עריכה פשוט רענון דפדפן (Ctrl+F5).
- **אירוח:** GitHub Pages מהרפו `RuthyRebirthing/ruthgutman-site` (branch `main`, שורש הרפו).
  קובץ [CNAME](CNAME) מפנה את הדומיין `ruthgutman.co.il` ל-Pages.
- **פרסום לאוויר = `git commit` + `git push` ל-`main`.** GitHub Pages מתעדכן תוך דקה-שתיים.
- כל דף HTML הוא עצמאי ומקושר ל-`assets/style.css`, `assets/a11y.css`, `assets/main.js`, `assets/a11y.js`.

> ⚠️ לפי ההוראות הגלובליות: אחרי כל שינוי בקוד — **לעצור לפני push ולשאול**, אלא אם המשתמש כתב במפורש "תעלה/תדחוף".

---

## 3. מבנה הקבצים

### דפי HTML (שורש הרפו) — 12 דפים

| קובץ | דף | תוכן עיקרי |
|------|-----|-----------|
| [index.html](index.html) | **בית** | הירו וידאו מסך-מלא + לוגו, אינטרו SEO, קטע "מפגש במתנה" (מקשר ל-gift.html) |
| [whatis.html](whatis.html) | מהו ריברסינג | הסבר, בלוק איור+טקסט, grid-3 "מה משתחרר". `body.bg-waves` |
| [poster.html](poster.html) | מתנת הנשימה | "מפת יתרונות" סימטרית: מדליון-לוגו מרכזי + 3 כרטיסים בכל צד + גלי SVG בתחתית |
| [articles.html](articles.html) | **מאמרים** | דף אינדקס: רשת כרטיסי מאמר (`.art-grid`/`.art-card`) + CTA. ראה §10 |
| [postpartum.html](postpartum.html) | מאמר · נשימה אחרי לידה | מאמר מלא + Article/BreadcrumbList schema |
| [teens.html](teens.html) | מאמר · ריברסינג לנערות | מאמר מלא + Article/BreadcrumbList schema |
| [about.html](about.html) | אודותי | ביו של רותי, badges, רצועת "למי מתאים", 6 חוות דעת |
| [faq.html](faq.html) | שאלות נפוצות | 5 שאלות ב-`<details>` אקורדיון + FAQ schema.org |
| [gift.html](gift.html) | **העניקו מתנה** | טופס שובר מתנה 3-שלבים (ראה §6). מכיל CSS+JS inline משלו |
| [contact.html](contact.html) | יצירת קשר | פרטי קשר + טופס Web3Forms (ראה §7) |
| [privacy.html](privacy.html) | מדיניות פרטיות | דף משפטי |
| [accessibility.html](accessibility.html) | הצהרת נגישות | דף משפטי (ת"י 5568) |

### assets/
| קובץ | תפקיד |
|------|-------|
| [assets/style.css](assets/style.css) | **כל העיצוב** (~1290 שורות, מחולק לסקשנים ממוספרים). ראה §5 |
| [assets/main.js](assets/main.js) | לוגיקת UI כללית (ראה §5.2) |
| [assets/mobile-onepage.js](assets/mobile-onepage.js) | **מובייל בלבד:** מרכז את כל דפי האתר בדף הבית (ראה §5.3) |
| [assets/a11y.css](assets/a11y.css) | עיצוב ווידג'ט הנגישות + הודעת עוגיות |
| [assets/a11y.js](assets/a11y.js) | ווידג'ט נגישות + cookie bar (ראה §8) |
| `logo_mark.png` | הלוגו (עיגול טורקיז). משמש גם כ-favicon וכ-og fallback |
| `video.mp4` + `video_poster.jpg` | וידאו ההירו של דף הבית (דחוס) |
| `waves-bg.jpg` | רקע ים לדפים עם `bg-waves` |
| `og_share.jpg` | תמונת שיתוף חברתי (1200×630) |

### google-apps-script/ — הבק-אנד של שובר המתנה
| קובץ | תפקיד |
|------|-------|
| [google-apps-script/Code.gs](google-apps-script/Code.gs) | **הקובץ האמיתי היחיד.** כל הלוגיקה של שובר המתנה (ראה §6) |
| [google-apps-script/הוראות-הקמה.md](google-apps-script/הוראות-הקמה.md) | מדריך הקמה/פריסה צעד-אחר-צעד |
| `Code_part1/part2/q1/q2/q2a/q2b.gs` | **פסולת דיבוג** — פיצולים ישנים של Code.gs מבדיקות "בידוד" (תקלת שמירה בעורך Apps Script). לא בשימוש, אפשר להתעלם/למחוק |

### קבצים שמורים / לא-באתר
- [.gitignore](.gitignore) מתעלם מ: `_unused/` (תמונות פוסטר ישנות), `מאמרים לעריכה/` + `מאמרים/` (מקורות ה-docx של רותי), וקבצים כבדים (`לוגו.png`, `דף הבית.mp4`, `be029251-...png`).
- [robots.txt](robots.txt) — Allow all + הפניה ל-sitemap.
- [sitemap.xml](sitemap.xml) — 12 דפים עם priority/lastmod. **לעדכן lastmod כשמשנים דף.**

---

## 4. תבנית משותפת לכל דף

כל דפי ה-HTML חולקים מבנה זהה. כשמוסיפים/משנים דף — לשמור על העקביות:

1. **`<html lang="he" dir="rtl">`** (דף הבית: גם `class="home"`).
2. **`<head>`** מלא SEO: `title`, `description`, `keywords`, `canonical`, Open Graph, Twitter card, `geo.region/placename`, favicon, `theme-color=#8FC8C2`. חלק מהדפים גם JSON-LD schema.org (`HealthAndBeautyBusiness`, `FAQPage`).
3. **`<header class="nav">`** — זהה בכל הדפים: לוגו+שם, טלפון, ניווט (8 קישורים), כפתור וואטסאפ (desktop+mobile), כפתור המבורגר. הקישור הפעיל מקבל `class="active"`.
   - ⚠️ ה-nav מודבק ידנית בכל דף (אין include). **שינוי בניווט = לעדכן ב-12 הדפים.**
4. **תוכן:** או `<section class="page-head">` (דפים פנימיים) או הירו ייעודי (בית/poster).
5. **`<footer>`** — וריאציות: `footer--center` (לוגו-lockup) או מינימלי.
6. **סקריפטים בסוף:** `main.js`, `a11y.js`, ואז `<script>` קטן שממלא `#yr` (שנה בפוטר).
7. **`class="reveal"`** על אלמנטים שאמורים להופיע בגלילה (עם `d1`/`d2` להשהיה מדורגת).

מספרי וואטסאפ ב-nav כוללים `?text=` עם הודעה מקודדת (URL-encoded): "היי רותי, אני מתעניינת בטיפול ריברסינג ואשמח לקבל פרטים".

---

## 5. מערכת העיצוב (assets/)

### 5.1 style.css — משתני `:root` וסקשנים
**פלטת צבעים (CSS vars):** טורקיז `--teal-900:#0C464B` → `--teal-300`, `--mint-100`, קרם (`--cream`, `--cream-soft`, `--ivory`), **זהב `--gold:#D9A94C`**, טקסט `--ink:#123B3E` / `--muted:#4A6E70`.
**גופן:** `--serif` ו-`--sans` שניהם `Tahoma, Arial, system-ui` (אין Google Fonts — הכל מקומי). `--maxw:1180px`, `--radius:22px`.

**סקשנים עיקריים ב-style.css (לפי הערות ממוספרות):** בסיס/טיפוגרפיה/כפתורים · nav · hero-video (דף בית) · page-head · buttons/btn-wa/btn-primary · reveal · cards/grid-3 · sec-mint · benefits (מפת poster, שורה ~520-670) · footer · **רספונסיביות** (טאבלט ≤, מובייל ≤760, צר ≤480) · **מוטיבים חיים** (רקע מים/גלים/בועות, שורה ~889+) · בלוקים (media-split, gold-emblem, ill-strip, video-box) · about/tst/faq/contact · form-toast.

**כפתורים:** `.btn-primary` (טורקיז), `.btn-wa` (ירוק וואטסאפ), `.btn-bit` (כחול ביט, ב-gift), `.btn-primary` עם svg אייקון.

### 5.2 main.js — נטען בכל דף
- **Navbar shrink** בגלילה (`.scrolled` אחרי 40px).
- **תפריט מובייל** (toggle `.open`).
- **Scroll reveal** דרך `IntersectionObserver` (מוסיף `.in` ל-`.reveal`).
- **FAQ accordion** — רק אחד פתוח בכל רגע (`<details class="faq-item">`).
- **Hero typewords** — כניסת מילים מדורגת בכותרת `h1[data-typewords]` (דף הבית).
- **מוטיבים חיים לדפים פנימיים בלבד** (`if (!isHome)`):
  - שכבת **בועות** מרחפות (`.decor-sky`).
  - **מים אמיתיים ב-WebGL** (shader caustics) שמוזרק כ-`<canvas class="head-water">` לתוך `.page-head` — **אלא אם** ל-body יש `bg-waves` (שם יש תמונת ים סטטית).
  - כיבוד `prefers-reduced-motion` (פריים סטטי), עצירה ב-`visibilitychange`.
  - poster.html נשאר **נקי** ממים בכוונה.

---

### 5.3 mobile-onepage.js — דף הבית כדף אחד ארוך (מובייל בלבד)

נוסף 27.08.2026. **נטען רק ב-index.html.** בפלאפון ובטאבלט דף הבית מרכז את
**כל שבעת דפי האתר** בגלילה אחת, והתפריט הופך לעוגנים בתוך הדף.
**במחשב שום דבר לא משתנה.**

⚠️ **תנאי ההפעלה: `(max-width:1080px)` וגם `(pointer:coarse)` — שניהם.**
בדיקת רוחב בלבד הפעילה את תצוגת המובייל גם במחשב: הגדלת תצוגה של Windows
(125%/150%) או zoom בדפדפן מורידים את הרוחב האפקטיבי מתחת לכל סף סביר.
`pointer:coarse` מזהה מסך מגע כמכשיר הראשי — מחשב נייד עם מסך מגע ועכבר
מדווח `fine` ולכן נשאר בתצוגה רגילה. **לבדיקה במחשב: `?onepage=1` בכתובת**
(ו-`?onepage=0` לכפיית ביטול).

הרקע לבקשה: הרבה מבקרים גוללים את דף הבית עד הסוף ויוצאים, בלי לפתוח את התפריט.

- **אין שכפול תוכן.** הסקשנים נשאבים בזמן אמת מקבצי ה-HTML הקיימים (`fetch` +
  `DOMParser`), אחרי אירוע `load`. **עריכה בדף מקורי מופיעה אוטומטית גם בדף
  הבית** — אין מה לעדכן פעמיים.
- **סדר הדפים** (מערך `PAGES` בראש הקובץ, לפי סדר התפריט):
  `whatis · poster · articles · about · faq · gift · contact`,
  עם מזהי עוגן `#m-whatis` … `#m-contact` (ו-`#top` להירו).
- מכל דף נלקח מה שבין ה-`<header>` ל-`<footer>`. ה-`<style>` שלו מוזרק ל-head
  וה-`<script>` שלו מורץ מחדש — **כך טופס שובר המתנה וטופס יצירת הקשר עובדים
  בתוך דף הבית**, על כל הלוגיקה שלהם.
- כל `h1` מוזרק הופך ל-`h2` (כותרת ראשית אחת לדף). לכן ב-style.css הסלקטורים
  הם `.page-head :is(h1,h2)` ו-`.benefits-head :is(h1,h2)` — **לא לצמצם חזרה
  ל-h1**, אחרת הכותרות בדף המאוחד מאבדות את עיצובן.

⚠️ **`html` מקבל `home-mob` במקום `home`** (סקריפט קצר ב-`<head>` של index.html,
לפני הציור הראשון). זה הלב של הפתרון: כל עיצוב הדפים הפנימיים כתוב תחת
`html:not(.home)`, ולכן הוא חל אוטומטית על התוכן המוזרק והוא נראה בדיוק כמו
בדף העצמאי שלו. כללי התפריט השקוף מעל הווידאו הורחבו ל-`:is(.home,.home-mob) .nav`.

⚠️ **שמות כיתות חייבים להיות ייחודיים בין דף לדף.** `.gift-card` היה קיים גם
ב-style.css (קטע "מפגש במתנה" בבית) וגם ב-CSS ה-inline של gift.html — ובדף
מאוחד הם דרסו זה את זה. שונה ב-gift.html ל-**`.voucher-card`**. לפני הוספת
CSS/JS inline לדף כלשהו — לוודא שהשם לא קיים כבר ב-style.css או בדף אחר.

⚠️ **ראשי הדפים המוזרקים מקבלים גלי SVG בלבד, בלי קנבס ה-WebGL** — שבעה קנבסי
מים באותו דף מרוקנים סוללה במובייל בלי להוסיף דבר.

⚠️ **`fetch` דורש שרת.** בפתיחת index.html ישירות מהדיסק (`file://`) ההזרקה לא
רצה והדף נשאר כפי שהוא — שום דבר לא נשבר, אבל **אי אפשר לבדוק ככה**. לבדיקה
מקומית: `python -m http.server 8000` ואז `http://localhost:8000`.

⚠️ **בדפים הפנימיים עצמם התפריט נשאר קישורים רגילים** (בכוונה): מי שנוחת על
`whatis.html` מגוגל לא נזרק לדף הבית.

⚠️ **הערת SEO:** גוגל מאנדקס mobile-first ומרנדר JS, ולכן יראה את תוכן כל
הדפים גם בדף הבית. ה-`canonical` של כל דף מצביע על עצמו, אך ייתכן שגוגל
יעדיף להציג את דף הבית לחלק מהשאילתות שקודם הובילו לדף הפנימי.

## 6. מערכת שובר המתנה (gift.html + Code.gs)

זה החלק המורכב היחיד. שני חלקים:

### 6.1 Frontend — [gift.html](gift.html)
טופס 3-שלבים (CSS+JS **inline** בקובץ עצמו, לא ב-assets):
- **שלב 1 (`#gift-step1`):** פרטי המתנה. שדות: שם רוכש, שם מקבל, **בורר "איך ברצונך לקבל את הקופון?"** (רדיו: מייל / וואטסאפ), שדה מותנה (מייל **או** טלפון לפי הבחירה), ברכה. כפתור "המשך לתשלום בביט".
- **שלב 2 (`#gift-step2`):** אחרי לחיצה — נפתח `BIT_LINK` בטאב חדש + כפתור **"שילמתי ✓"**. (הוסר בכוונה קישור "חזרה לתיקון הפרטים".)
- **שלב 3 (`#gift-step3`):** הודעת הצלחה. הטקסט מתעדכן דינמית לפי אופן הקבלה ("יישלח למייל/לוואטסאפ שהזנת"). לב ירוק 💚 תואם לוי הירוק.

**מנגנון השליחה (עוקף CORS):** טופס האישור (`#gift-confirm`) נשלח ב-POST ל-`<iframe name="gift-sink">` נסתר → הדף לא מתחלף, וה-`load` של ה-iframe מפעיל את מעבר שלב 3.

**לוגיקת JS מרכזית ב-gift.html:**
- `syncDelivery()` — מציג/מסתיר שדה מייל מול טלפון לפי הרדיו `deliveryMethod`.
- `validate()` — מוודא **רק שדות מוצגים** (מדלג על שדה מוסתר דרך `offsetParent === null`).
- **זיכרון פרטי הרוכש (`localStorage` מפתח `rg_gift_buyer`):** שם/מייל/טלפון/אופן-קבלה נשמרים בעת "המשך לתשלום" וממולאים אוטומטית בכניסה הבאה. נחוץ כי הטופס משתמש ב-`preventDefault()` → הדפדפן לא "לומד" את הערכים לבד. שם המקבל **לא** נשמר (משתנה בכל מתנה).
- **`.gift-card.is-confirm`** — class ש-JS מוסיף במעבר לשלב 2, מקטין את פדינג הכרטיס כדי שמסכי האישור (שילמתי/הצלחה) יהיו צמודים לתוכן בלי רווח מיותר.
- **2 ערכים למילוי בראש הסקריפט:** `BIT_LINK` (קישור בקשת תשלום בביט 250₪) ו-`SCRIPT_URL` (כתובת `/exec` של Apps Script). כרגע placeholders `REPLACE_WITH_...`.

### 6.2 Backend — [google-apps-script/Code.gs](google-apps-script/Code.gs)
Google Apps Script Web App, מחובר ל-Google Sheet. **קובץ נפרד — לא מתפרסם ב-GitHub Pages.** פריסה ידנית (ראה הוראות-הקמה.md).

- **`CFG`** בראש הקובץ: `RUTHY_EMAIL`, `RUTHY_WHATSAPP`, `CALLMEBOT_APIKEY`, `START_SERIAL:1150`, `VALID_MONTHS:6`, `AMOUNT:250`, שם/טלפון/אתר.
- **`doPost`** — קולט הזמנה, מוסיף שורה לגיליון "הזמנות" (סטטוס "ממתין" + token), קורא ל-`notifyRuthy`.
- **`notifyRuthy`** — שולח לרותי **מייל** עם כפתור "אשר ושלח" + **וואטסאפ** דרך CallMeBot. מציג את **אופן הקבלה** שהרוכש בחר.
- **`doGet`** עם `action=approve` → **`handleApprove`**: מנפיק מספר סידורי רץ (`nextSerial`, נשמר ב-ScriptProperties), מסמן "אושר", **שולח מייל קופון רק אם יש `buyerEmail`** (הזמנת וואטסאפ → רותי שולחת ידנית בכפתור). `action=coupon` → `showCoupon` (דף הורדה/הדפסה PDF).
- **`couponInline(d)`** — עיצוב הקופון (טבלאי, תואם-מייל). מלמעלה למטה: פס עליון, "לכבוד" + שם מקבל, ברכה, שורת חתימה "באהבה, ליהנות מכל נשימה `&#9829;`" (לב מעוגן RTL עם `&#8207;`), קו מנוקב, **מספר סידורי בולט וממורכז**, ואז **תוקף קטן בשורה נפרדת הכי למטה**. ברגל: **לוגו** (`logo_mark.png` מהכתובת הציבורית) + שם העסק + טלפון + **כתובת האתר כקישור** (`<a href>` ל-`ruthgutman.co.il`).
  - ⚠️ **RTL בקופון:** הלב עוגן לסוף המשפט (שמאל) עם סימני כיווניות `&#8207;` (RLM) משני הצדדים — בלי זה הלב "קופץ" להתחלה בחלק מלקוחות המייל.
- **מספור סידורי:** ראשון = 1150, כל אישור +1. מספרים **לא נשרפים** על הזמנות שלא אושרו.
- **זרימה יומיומית לרותי:** לקוח משלם בביט → רותי מקבלת מייל+וואטסאפ → בודקת שהכסף נכנס → "אשר ושלח" → קופון נשלח אוטומטית.

> אחרי כל שינוי ב-Code.gs צריך **פריסה מחדש** ידנית ב-Apps Script (Deploy → Manage deployments → New version). שינוי מקומי בקובץ **לא** מספיק.

---

## 7. טופס יצירת קשר (contact.html)
- שולח ל-**Web3Forms** (`api.web3forms.com/submit`, `access_key` בתוך ה-HTML) דרך `<iframe name="cf-sink">` נסתר (אותו טריק כמו gift).
- שדות: שם מלא, טלפון, הודעה, **checkbox הסכמה למדיניות פרטיות (חובה)**, מלכודת ספאם (`botcheck` נסתר), ותיעוד מועד+מקור ההסכמה (hidden).
- ה-JS מציג toast "ההודעה נשלחה" ל-6 שניות ומאפס את הטופס.

---

## 8. נגישות (a11y.js / a11y.css)
תואם **ת"י ישראלי 5568 רמה AA**. נטען בכל דף, לא נוגע בעיצוב הקיים:
- **ווידג'ט צף** (`.a11y-fab`) שפותח פאנל: הגדלת טקסט (4 דרגות 100→125%), ניגודיות גבוהה, הדגשת קישורים, עצירת אנימציות, גופן קריא ומרווח, איפוס.
- ההעדפות נשמרות ב-`localStorage` (`rg_a11y_v2`), מוחלות כ-classes על `<html>` (`a11y-contrast` וכו').
- **skip-link** "דילוג לתוכן", תמיכת מקלדת (Esc/פוקוס), ARIA מלא.
- **הודעת עוגיות** (`rg_cookie_consent` ב-localStorage) — מוצגת פעם אחת.

---

## 9. כללי עבודה חשובים

- **עברית + RTL תמיד.** בכל תוכן/מייל/קופון לוודא כיווניות (סימני `&#8207;`/`&#8206;` איפה שסמלים/מספרים קופצים).
- **שדות מספר:** `placeholder="0"` ולא `value="0"` (ריק = 0 בחישוב).
- **ניווט/פוטר משוכפלים ב-12 דפים** — שינוי גורף = לעדכן בכולם (שקול sed/סקריפט).
- **sitemap.xml** — לעדכן `lastmod` לדף ששונה.
- **אין build.** לבדוק ע"י פתיחת הקובץ בדפדפן. פרודקשן = commit+push ל-`main` (GitHub Pages).
- **git:** branch `main`, remote `origin` = `RuthyRebirthing/ruthgutman-site`. לשאול לפני push (אלא אם נאמר "תעלה").
- **Code.gs דורש פריסה ידנית נפרדת** — לא חלק מ-push ל-Pages.

---

## 10. מערכת המאמרים (articles.html + דפי מאמר)

נוספה 27.07.2026. **המקור:** רותי כותבת מאמר ב-Word לפי תבנית קבועה (טבלת מטא בראש הקובץ + טקסט). קבצי ה-docx יושבים ב-`מאמרים/` ו**מוחרגים ב-.gitignore** — התוכן החי הוא ה-HTML בלבד.

### 10.1 מבנה
- [articles.html](articles.html) — **דף אינדקס**. `.art-grid` (2 עמודות, עמודה אחת ≤900px) עם `<a class="art-card">` — כל הכרטיס הוא לינק. מבנה כרטיס: `.art-card__top` (רקע טורקיז + `.art-card__ico` אייקון SVG זהב + `.art-card__tag` תגית + `.art-card__wave` גלי SVG שמתמזגים לגוף הכרטיס) ואז `.art-card__body` (h2 · תקציר · `.art-card__foot` עם זמן קריאה + "לקריאת המאמר").
  - ⚠️ הגל התחתון בכרטיס מצויר עם `fill="#FBFEFD"` **קשיח** כדי להתמזג עם `--ivory` של גוף הכרטיס. אם משנים את רקע `.art-card` — לעדכן גם את ה-fill ב-SVG.
- [postpartum.html](postpartum.html) / [teens.html](teens.html) — **דפי מאמר**. `page-head` רגיל (מקבל מים+גלים מ-main.js), ואז `.article-wrap` ובתוכו: `.art-crumbs` (פירורי לחם) · `article.article` (גיליון זכוכית) · `.art-cta` (באנר טורקיז) · `.art-next` (קישורים למאמרים אחרים).

### 10.2 בלוקים לשימוש חוזר בתוך `.article`
| class | שימוש |
|-------|-------|
| `.art-meta` | שורת "מאת · זמן קריאה · אשדוד" |
| `.art-toc` | תוכן עניינים ממוספר (עוגנים ל-`h2 id=...`) |
| `.art-lead` | פסקת פתיחה מודגשת |
| `.art-story` + `.art-story__lbl` + `.qt` | בלוק "מהקליניקה" (ציטוט מטופלת) |
| `.art-quotes` | צ'יפים של ציטוטים קצרים |
| `.art-warn` | בלוק אזהרה רפואית (זהב-כתום) |
| `.art-note` | בלוק מידע/הבהרה (טורקיז בהיר) |
| `.art-mini` | 3 כרטיסי משנה עם אייקון |
| `.art-list` | רשימה עם תבליט טיפת-נשימה |
| `.art-sign` | חתימה: לוגו + "באהבה, רותי גוטמן" + טלפון |
| `.art-disclaimer` | הסתייגות משפטית — **חובה בכל מאמר** (E-E-A-T/גוגל בריאות) |
| `<details class="faq-item">` | שאלות בתוך מאמר — נהנה מהאקורדיון של main.js |

### 10.3 להוספת מאמר חדש
1. לשכפל `postpartum.html` → לשנות שם קובץ, `<title>`/description/keywords/canonical/OG לפי **טבלת המטא בראש קובץ ה-Word**.
2. לעדכן את שני בלוקי ה-JSON-LD (Article + BreadcrumbList).
3. להוסיף כרטיס `.art-card` ב-articles.html + ערך ב-`hasPart` של ה-JSON-LD שם.
4. להוסיף קישור ב-`.art-next` של המאמרים האחרים.
5. להוסיף `<url>` ב-sitemap.xml.
6. `<article class="article reveal in">` — **חובה `in`**, אחרת בלוק ארוך לא עובר את סף ה-IntersectionObserver ונשאר שקוף.

> ממתין: המאמר השלישי **"ריברסינג לילדים"** (`kids.html`) — הטיוטה קיימת ב-`מאמרים לעריכה/1 - ריברסינג לילדים.docx` אך רותי טרם ערכה אותו.

---

## 11. סרטון פרומו לסטטוס וואטסאפ (`promo/`)

נוסף 17.08.2026. **לא חלק מהאתר** — קובץ שיווקי להפצה. ה-mp4 וה-jpg מוחרגים ב-.gitignore; רק הסקריפטים נשמרים.

**התוצר:** `promo/promo_ruthgutman.mp4` — 1080×1920, 30fps, 23.9 שניות, H.264+AAC, ~9.6MB (מתחת למגבלת סטטוס וואטסאפ).
מבנה: הירו וידאו הים (0-4.6s) → כרטיס שם (4.6-6.9s) → 4 "הבזקים" של דפי האתר בתוך mockup טלפון עם גלילה (6.9-18.1s: whatis · articles · about · faq) → כרטיס CTA עם הכתובת והטלפון (18.1-23.9s).

⚠️ **דף שובר המתנה (gift) הוצא מהסרטון בכוונה** לבקשת רותי — לא להחזיר. האוברליי שלו (`overlays.html?i=9`) נשאר בקובץ אך אינו בשימוש.
⚠️ **הלוגו באוברליים משתמש ב-`filter:drop-shadow` ולא ב-`box-shadow`** — ה-PNG שקוף, ו-box-shadow צייר "טבעת" ריבועית סביבו שנראתה ככתם. יש גם `drop-shadow(0 0 36px …)` כהילה, אחרת הלוגו הבהיר נבלע בשמיים הבהירים של הווידאו.
⚠️ **קצב הגלים — `SPEED_HERO` / `SPEED_BG` בראש `build.py`.** בקצב מלא (1.0) הים מרגיש מלחיץ; הערכים כיום 0.58 לפתיח ו-0.26 לרקע המטושטש. `video_frame()` ו-`blur_bg()` מקבלים **מיקום רציף** ועושים מיזוג בין שני פריימים סמוכים — בלי זה ההאטה נראית מדורגת.

**תמונת הסטטוס הנלווית:** `promo/share_card.jpg` (נבנית מ-`share_card.html`) — 1080×1920, **עולה לסטטוס מיד אחרי הסרטון** ובכיתוב שלה הקישור הלחיץ לאתר. מעוצבת במראה של דף `whatis.html`: רקע `waves-bg.jpg`, טקסט טורקיז כהה עם צל לבן, כרטיס זכוכית. הסרטון כהה והתמונה בהירה — ניגוד מכוון.

⚠️ בסרטון עצמו **אין כתובת אתר** — במקומה "חפשו בגוגל · רותי גוטמן ריברסינג" (בסטטוס וידאו אין קישור לחיץ). הקישור מופיע רק בתמונה ובכיתוב שלה.
⚠️ **קובץ תמונה לא יכול להכיל קישור לחיץ** — זו מגבלת פורמט (JPG/PNG = פיקסלים בלבד). לכן בתמונה יש **קוד QR** (`qr.png`, נוצר ב-`segno` עם `error='h'`, 186px בתמונה) + חץ שמפנה לכיתוב. הקישור הלחיץ נכנס בכיתוב הסטטוס בוואטסאפ. ה-QR נבדק שהוא מפוענח גם אחרי דחיסת JPEG אגרסיבית.

**שרשרת הבנייה (להרצה בסדר הזה, דורש ffmpeg + Chrome + numpy/PIL):**
| שלב | קובץ | מה עושה |
|-----|------|---------|
| 1 | `shoot.py` | מעתיק את האתר לתיקיית עבודה, מזריק CSS (`promo-fix`) ומצלם full-page ב-Chrome headless |
| 2 | `overlays.html` | כל שכבות הטקסט (`?i=1..14`) — נצלמות ל-PNG שקוף עם `--default-background-color=00000000` |
| 3 | `build.py` | מרכיב ב-PIL פריים-פריים ומזרים ל-ffmpeg (`_video_only.mp4`) |
| 4 | `audio.py` | מייצר פסקול אמביינטי ב-numpy (`_audio.wav`) — פד + פעימות נשימה + פעמונים |
| 5 | ffmpeg mux | `-c:v libx264 -crf 22 -maxrate 5000k -c:a aac -b:a 160k` |

⚠️ **שתי מלכודות בצילום הדפים** (מטופלות ב-`shoot.py`, לא לשבור):
- `html{zoom:.8}` שב-style.css משבש את קואורדינטות הצילום ב-RTL וחותך את הטקסט → ההזרקה מבטלת אותו (`zoom:1`) ומצלמת ברוחב 540 CSS px במקום 430.
- אלמנטים בגובה `100vh` נמתחים לגובה החלון הענק → ההזרקה מקבעת אותם ל-1000px.

`build.py` חותך אוטומטית את השטח הריק שמתחת לתוכן הצילום (`trim_shot`, לפי שונות אופקית בכל שורה) — בלי זה ה-pan "נופל" לרקע ריק בסוף הדף.
